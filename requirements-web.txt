# Optional web application dependencies. The legacy pipeline and CLI continue
# to use only the Python standard library.
fastapi>=0.115,<1
uvicorn>=0.30,<1
httpx>=0.27,<1
python-multipart>=0.0.31,<1
pypdf>=5,<7
reportlab>=4.4,<5
pillow>=12.3,<13
psycopg[binary]>=3.2,<4
# Only Fernet is used. 46 is the floor because newer releases publish no Intel
# macOS wheels; requirements-web.lock still pins the tested version.
cryptography>=46
starlette>=1.3.1,<2
openai>=3.3,<4
anthropic>=0.104,<1
